import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("메인 화면")
                .font(.title)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    HomeView()
} 
